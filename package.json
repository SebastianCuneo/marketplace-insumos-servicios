{
  "name": "marketplace-insumos-servicios",
  "version": "1.0.0",
  "description": "Marketplace for supplies and services - Monorepo (Web + Mobile)",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "npm run start --workspace=@marketplace/web",
    "mobile": "npm run start --workspace=@marketplace/mobile",
    "web:build": "npm run build --workspace=@marketplace/web",
    "mobile:build": "npm run build --workspace=@marketplace/mobile"
  },
  "keywords": ["react", "react-native", "expo", "marketplace", "monorepo"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
